# NodeJS and React Module assignment 5

## How to start

backend: in a terminal, navigate to /server and run the commands "npm install" and "node server.js"
frontend: in a new terminal, seperate from the back-end server terminal, navigate to .\client\vite-project and run the commands "npm install" and "npm run dev".

## Components and features

### Components

### Features

## Dependencies

## DevDependencies
